buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'io.dcow.gradle:sublime:0.9.+'
    }
}

group = 'io.dcow'
version = "0.9.0"

apply plugin: 'io.dcow.sublime'

task wrapper(type: Wrapper) {
    gradleVersion '2.2.1'
}